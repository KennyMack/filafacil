'use strict';
(function(angular){
  angular.module('filafacil')
    .controller('mainController', [
      '$rootScope',
      function($rootScope){
        $rootScope.messages = [];


      }]);
}(angular));
